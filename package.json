{
  "dependencies": {
    "@slack/client": "^4.6.0",
    "request": "^2.88.0",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "aws-sdk": "^2.330.0"
  },
  "scripts": {
    "package": "docker-compose run package && docker-compose down"
  }
}
